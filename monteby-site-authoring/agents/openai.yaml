interface:
  display_name: "Monteby Site Authoring"
  short_description: "Route and verify canonical Monteby site work"
  brand_color: "#111827"
  default_prompt: "Use $monteby-site-authoring to select exactly one base mode, follow the reported nextAction, and return a live-contract-backed verdict without raw markup, copied external content, or embedded secrets."
